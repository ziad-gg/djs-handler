const fs = require('fs');

class Application {
  constructor() {
    
  }
  
  async _patch(obj, key, value) {
    const Object_c = new Object();
    Object_c[key] = value;
    const main = this
    Object.defineProperty(main, key.toString?.(), {
      value: 
    })
  }
  
  async build() {
    
  }
}